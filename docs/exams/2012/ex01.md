---
id: ex-2012-01
year: 2012
exam: final
tags:
  - ordenación
  - complejidad
---

# Ejercicio 1 — Ordenación por índices (3 puntos)

## Enunciado

Se pide escribir un algoritmo:

```java
int[] ordena(long[] datos)
```

que devuelva un array de índices indicando en qué posición iría cada dato al ordenar en orden ascendente.

---

## a) Implementación iterativa (1.5 puntos)

??? note "Mostrar solución"
    ```java
    public int[] sort(long[] datos) {
        int[] indices = new int[datos.length];
        Arrays.fill(indices, -1);
        for (int i = 0; i < indices.length; i++) {
            int iMenor = -1;
            long menor = Long.MAX_VALUE;
            for (int j = 0; j < datos.length; j++) {
                if (indices[j] < 0 && datos[j] < menor) {
                    iMenor = j;
                    menor = datos[j];
                }
            }
            indices[iMenor] = i;
        }
        return indices;
    }
    ```

---

## b) Complejidad (1.5 puntos)

??? note "Mostrar solución"
    Hay dos bucles anidados. El número total de comparaciones es:

    ```
    (N-1) + (N-2) + ... + 1 = O(N^2)
    ```
